libgenders      C API to operate on /etc/genders

Libgenders.pm   Perl extensions interface to the C API

Genders.pm      Perl interface to libgenders

nodeattr	query genders attributes - see nodeattr(1)

subpackage compat

gendlib.pl      genders perl api for backwards compatability 
