#ifndef _GENDERSLOCAL_H
#define _GENDERSLOCAL_H

/* The following are a small set of helper functions
 * that will allow developers to develop a local
 * genders API for their own specific needs.  
 */

void genders_set_errnum(genders_t handle, int errnum);

#endif /* _GENDERSLOCAL_H */
